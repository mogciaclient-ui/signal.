'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {signInWithEmailAndPassword} from 'firebase/auth';import {firebaseServices,isFirebaseConfigured} from '../../lib/firebase/client';
export default function Login(){const router=useRouter();const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=new FormData(e.currentTarget);try{if(!isFirebaseConfigured)throw new Error('Firebase設定が未登録です。環境変数を設定してください。');await signInWithEmailAndPassword(firebaseServices().auth,String(data.get('email')),String(data.get('password')));router.push('/');}catch(e){setError(e instanceof Error?e.message:'ログインに失敗しました。')}finally{setLoading(false)}}return <main className="login-page"><section className="login-brand"><Link href="/" className="brand">Signal<span>.</span></Link><div><p>PLAN · CREATE · PUBLISH · IMPROVE</p><h1>日々のInstagram運用を、<br/>ひとつの流れに。</h1><span>投稿、予約、分析まで。迷わず続けられる運用ワークスペース。</span></div><small>© 2026 Signal.</small></section><section className="login-panel"><form className="login-card" onSubmit={submit}><p className="eyebrow">WELCOME BACK</p><h2>Signal.へログイン</h2><p>登録済みのメールアドレスでログインしてください。</p>{error&&<div className="form-error" role="alert">{error}</div>}<label>メールアドレス<input name="email" type="email" autoComplete="email" required/></label><label>パスワード<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" type="submit" disabled={loading}>{loading?'ログインしています…':'ログイン'}</button><small>Firebase Authenticationで安全に認証します</small><nav><Link href="/privacy">Privacy</Link><Link href="/terms">Terms</Link><Link href="/data-deletion">Data Deletion</Link></nav></form></section></main>}
