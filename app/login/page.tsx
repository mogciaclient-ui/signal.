'use client';
import {useState} from 'react';import Link from 'next/link';
export default function Login(){const [loading,setLoading]=useState(false);const [error,setError]=useState('');return <main className="login-page"><section className="login-brand"><Link href="/" className="brand">Signal<span>.</span></Link><div><p>PLAN · CREATE · PUBLISH · IMPROVE</p><h1>日々のInstagram運用を、<br/>ひとつの流れに。</h1><span>投稿、予約、分析まで。迷わず続けられる運用ワークスペース。</span></div><small>© 2026 Signal.</small></section><section className="login-panel"><form className="login-card" onSubmit={e=>{e.preventDefault();setLoading(true);setError('');setTimeout(()=>setLoading(false),700)}}><p className="eyebrow">WELCOME BACK</p><h2>Signal.へログイン</h2><p>登録済みのメールアドレスでログインしてください。</p>{error&&<div className="form-error" role="alert">{error}</div>}<label>メールアドレス<input type="email" defaultValue="reviewer@signal.jp" required/></label><label>パスワード<input type="password" defaultValue="signal-review" required/></label><button className="primary" type="submit">{loading?'確認しています…':'ログイン'}</button><button type="button" className="text-button" onClick={()=>setError('メールアドレスまたはパスワードを確認してください。')}>エラー表示を確認</button><small>審査用アカウントでログインできます</small><nav><Link href="/privacy">Privacy</Link><Link href="/terms">Terms</Link><Link href="/data-deletion">Data Deletion</Link></nav></form></section></main>}
