'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {signInWithEmailAndPassword} from 'firebase/auth';import {firebaseServices,isFirebaseConfigured} from '../../lib/firebase/client';
export default function Login(){const router=useRouter();const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=new FormData(e.currentTarget);try{if(!isFirebaseConfigured)throw new Error('Firebase is not configured.（Firebase設定が未登録です）');await signInWithEmailAndPassword(firebaseServices().auth,String(data.get('email')),String(data.get('password')));router.push('/');}catch(e){setError(e instanceof Error?e.message:'Login failed.（ログインに失敗しました）')}finally{setLoading(false)}}return <main className="login-page"><section className="login-brand"><Link href="/" className="brand">Signal<span>.</span></Link><div><p>PLAN · CREATE · PUBLISH · IMPROVE</p><h1>Manage Instagram<br/>in one seamless flow.</h1><span>Plan, publish, schedule, and analyze—all in one workspace.<br/>投稿・予約・分析を、ひとつのワークスペースで。</span></div><small>© 2026 Signal.</small></section><section className="login-panel"><form className="login-card" onSubmit={submit}><p className="eyebrow">WELCOME BACK</p><h2>Sign in to Signal.（ログイン）</h2><p>Use your registered email address.（登録済みのメールアドレスを使用してください）</p>{error&&<div className="form-error" role="alert">{error}</div>}<label>Email address（メールアドレス）<input name="email" type="email" autoComplete="email" required/></label><label>Password（パスワード）<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" type="submit" disabled={loading}>{loading?'Signing in…（ログイン中）':'Sign In（ログイン）'}</button><small>Secure authentication by Firebase（Firebaseで安全に認証）</small><nav><Link href="/privacy">Privacy</Link><Link href="/terms">Terms</Link><Link href="/data-deletion">Data Deletion</Link></nav></form></section></main>}
