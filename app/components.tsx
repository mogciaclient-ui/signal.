"use client";
import Link from "next/link";
import { AuthGuard } from "./auth-guard";
import {useSignalData} from "../lib/firebase/data";
export function AppShell({children,active,title,action}:{children:React.ReactNode;active:string;title:string;action?:React.ReactNode}){const nav=[['/','⌂','ホーム'],['/posts','▦','投稿'],['/analytics','⌁','分析'],['/scheduled','◷','投稿管理']];const{account,loading}=useSignalData();return <AuthGuard><main className="shell"><aside className="sidebar"><Link href="/" className="brand">Signal<span>.</span></Link><nav aria-label="メインナビゲーション">{nav.map(([href,icon,label])=><Link key={href} href={href} className={`nav-item ${active===href?'active':''}`}><span>{icon}</span>{label}</Link>)}</nav><nav className="bottom-nav"><Link href="/review" className={`nav-item ${active==='/review'?'active':''}`}><span>✓</span>審査準備</Link><Link href="/settings/instagram" className={`nav-item ${active==='/settings/instagram'?'active':''}`}><span>⚙</span>設定</Link><Link href="/settings/instagram" className="profile"><div className="avatar">{account?.username?.slice(0,1).toUpperCase()||'S'}</div><div><strong>Instagram</strong><small>{loading?'読込中…':account?`@${account.username}`:'未接続'}</small></div><span>⌄</span></Link></nav></aside><section className="content"><header className="topbar"><div><p className="eyebrow">INSTAGRAM WORKSPACE</p><h1>{title}</h1></div>{action}</header>{children}</section></main></AuthGuard>}
