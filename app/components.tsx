"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { AuthGuard } from "./auth-guard";
import {useSignalData} from "../lib/firebase/data";
export function AppShell({children,active,title,action}:{children:React.ReactNode;active:string;title:string;action?:React.ReactNode}){const nav=[['/','⌂','ホーム'],['/posts','▦','投稿'],['/analytics','⌁','分析'],['/scheduled','◷','予約投稿']];const{account}=useSignalData();return <AuthGuard><main className="shell"><aside className="sidebar"><a href="/" className="brand">Signal<span>.</span></a><nav aria-label="メインナビゲーション">{nav.map(([href,icon,label])=><a key={href} href={href} className={`nav-item ${active===href?'active':''}`}><span>{icon}</span>{label}</a>)}</nav><nav className="bottom-nav"><a href="/review" className={`nav-item ${active==='/review'?'active':''}`}><span>✓</span>審査準備</a><a href="/settings/instagram" className={`nav-item ${active==='/settings/instagram'?'active':''}`}><span>⚙</span>設定</a><a href="/settings/instagram" className="profile"><div className="avatar">{account?.username?.slice(0,1).toUpperCase()||'S'}</div><div><strong>Instagram</strong><small>{account?`@${account.username}`:'未接続'}</small></div><span>⌄</span></a></nav></aside><section className="content"><header className="topbar"><div><p className="eyebrow">INSTAGRAM WORKSPACE</p><h1>{title}</h1></div>{action}</header>{children}</section></main></AuthGuard>}
