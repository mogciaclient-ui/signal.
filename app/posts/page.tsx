import Link from 'next/link';import {AppShell,demoPosts} from '../components';
export default function Posts(){return <AppShell active="/posts" title="投稿"><div className="toolbar"><p>Instagramから取得した投稿</p><button className="secondary">↻ Instagramから同期</button></div><div className="post-list">{demoPosts.map(p=><Link href={`/posts/${p.id}`} className="post-row" key={p.id}><div className={`thumb ${p.color}`}>Signal.</div><div className="grow"><small>{p.date} ・ {p.type}</small><strong>{p.title}</strong></div><div className="row-stat"><small>リーチ</small><b>{p.reach}</b></div><div className="row-stat"><small>いいね</small><b>{p.likes}</b></div><div className="row-stat"><small>コメント</small><b>{p.comments}</b></div><span>›</span></Link>)}</div></AppShell>}
